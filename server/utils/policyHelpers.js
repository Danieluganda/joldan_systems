/**\n * Policy Helpers Utility\n * \n * Helper functions for policy and permission evaluation\n * Feature 5: Role-based access control\n */\n\nconst { PERMISSIONS, hasPermission } = require('../config/permissions');\n\n/**\n * Check if user can create procurement\n */\nconst canCreateProcurement = (userRole) => {\n  return hasPermission(userRole, PERMISSIONS.CREATE_PROCUREMENT);\n};\n\n/**\n * Check if user can approve procurement\n */\nconst canApprove = (userRole) => {\n  return hasPermission(userRole, PERMISSIONS.APPROVE_SUBMISSIONS);\n};\n\n/**\n * Check if user can evaluate bids\n */\nconst canEvaluate = (userRole) => {\n  return hasPermission(userRole, PERMISSIONS.CREATE_EVALUATIONS);\n};\n\n/**\n * Check if user can view audit trail\n */\nconst canViewAudit = (userRole) => {\n  return hasPermission(userRole, PERMISSIONS.VIEW_AUDIT);\n};\n\n/**\n * Check if user can publish RFQ\n */\nconst canPublishRFQ = (userRole) => {\n  return hasPermission(userRole, PERMISSIONS.PUBLISH_RFQ);\n};\n\n/**\n * Check if user can manage users (admin only)\n */\nconst canManageUsers = (userRole) => {\n  return hasPermission(userRole, PERMISSIONS.MANAGE_USERS);\n};\n\n/**\n * Get user capabilities\n */\nconst getUserCapabilities = (userRole) => {\n  return {\n    canCreateProcurement: canCreateProcurement(userRole),\n    canApprove: canApprove(userRole),\n    canEvaluate: canEvaluate(userRole),\n    canViewAudit: canViewAudit(userRole),\n    canPublishRFQ: canPublishRFQ(userRole),\n    canManageUsers: canManageUsers(userRole),\n  };\n};\n\nmodule.exports = {\n  canCreateProcurement,\n  canApprove,\n  canEvaluate,\n  canViewAudit,\n  canPublishRFQ,\n  canManageUsers,\n  getUserCapabilities,\n};\n"
