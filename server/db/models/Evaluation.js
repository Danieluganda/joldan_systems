/**\n * Evaluation Model\n * \n * Data access layer for bid evaluation records\n * Feature 6: Evaluation and scoring\n */\n\nconst pool = require('../config/database');\n\nclass Evaluation {\n  /**\n   * Create a new evaluation\n   */\n  static async create({\n    submissionId,\n    evaluatorId,\n    status,\n    technicalScore,\n    financialScore,\n    overallScore,\n    comments,\n  }) {\n    const query = `\n      INSERT INTO evaluations (submission_id, evaluator_id, status, technical_score, financial_score, overall_score, comments, created_at)\n      VALUES ($1, $2, $3, $4, $5, $6, $7, NOW())\n      RETURNING *\n    `;\n    const result = await pool.query(query, [\n      submissionId,\n      evaluatorId,\n      status,\n      technicalScore,\n      financialScore,\n      overallScore,\n      comments,\n    ]);\n    return result.rows[0];\n  }\n\n  /**\n   * Get evaluation by ID\n   */\n  static async findById(id) {\n    const query = 'SELECT * FROM evaluations WHERE id = $1';\n    const result = await pool.query(query, [id]);\n    return result.rows[0];\n  }\n\n  /**\n   * Get all evaluations\n   */\n  static async getAll(filters = {}) {\n    let query = 'SELECT * FROM evaluations WHERE 1=1';\n    const values = [];\n    let paramCount = 1;\n\n    if (filters.submissionId) {\n      query += ` AND submission_id = $${paramCount}`;\n      values.push(filters.submissionId);\n      paramCount++;\n    }\n\n    if (filters.evaluatorId) {\n      query += ` AND evaluator_id = $${paramCount}`;\n      values.push(filters.evaluatorId);\n      paramCount++;\n    }\n\n    if (filters.status) {\n      query += ` AND status = $${paramCount}`;\n      values.push(filters.status);\n      paramCount++;\n    }\n\n    query += ' ORDER BY created_at DESC';\n    const result = await pool.query(query, values);\n    return result.rows;\n  }\n\n  /**\n   * Update evaluation\n   */\n  static async update(id, updates) {\n    const keys = Object.keys(updates);\n    const values = Object.values(updates);\n    const setClauses = keys.map((key, i) => `${key} = $${i + 1}`).join(', ');\n    const query = `\n      UPDATE evaluations\n      SET ${setClauses}, updated_at = NOW()\n      WHERE id = $${keys.length + 1}\n      RETURNING *\n    `;\n    values.push(id);\n    const result = await pool.query(query, values);\n    return result.rows[0];\n  }\n\n  /**\n   * Delete evaluation\n   */\n  static async delete(id) {\n    const query = 'DELETE FROM evaluations WHERE id = $1';\n    await pool.query(query, [id]);\n  }\n\n  /**\n   * Get evaluations by submission\n   */\n  static async getBySubmission(submissionId) {\n    const query = 'SELECT * FROM evaluations WHERE submission_id = $1 ORDER BY created_at DESC';\n    const result = await pool.query(query, [submissionId]);\n    return result.rows;\n  }\n\n  /**\n   * Get evaluations by evaluator\n   */\n  static async getByEvaluator(evaluatorId) {\n    const query = 'SELECT * FROM evaluations WHERE evaluator_id = $1 ORDER BY created_at DESC';\n    const result = await pool.query(query, [evaluatorId]);\n    return result.rows;\n  }\n}\n\nmodule.exports = Evaluation;\n"
